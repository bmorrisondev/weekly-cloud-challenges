apiVersion: v1
kind: Service
metadata:
    name: "cloudchallengeakskub"
    labels:
        app: "cloudchallengeakskub"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "cloudchallengeakskub"